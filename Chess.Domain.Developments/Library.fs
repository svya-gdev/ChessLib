namespace Chess.Domain

module Developments =
    let Palfrey : PawnDevelopment = {
        Advances = Set.empty
        Captures = Set.empty
    }
    let Destrier : PawnDevelopment = {
        Advances = Set.empty
        Captures = Set.empty
    }
    let Courser : PawnDevelopment = {
        Advances = Set.empty
        Captures = Set.empty
    }